<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="4dp"
        android:color="#070707" /> <!-- Default border color -->
    <corners android:radius="0dp" /> <!-- Default corner radius -->
</shape>
